# DolphinScheduler Agent Dependencies
# Updated for compatibility

# LangChain (LLM Agent) - 使用稳定的 0.3.x 或更高版本
# 注意: langchain 0.3.x 是独立包，langchain-core 是核心包
langchain>=0.3.0,<1.0.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
langgraph>=0.2.0

# DingTalk Stream
dingtalk-stream>=0.24.0

# HTTP & API
fastapi>=0.100.0
uvicorn>=0.23.0
requests>=2.31.0
pydantic>=2.0.0

# Environment & Config
python-dotenv>=1.0.0
pyyaml>=6.0

# Kubernetes (可选)
# kubernetes>=28.0.0

# Development
pytest>=7.0.0

# Graph Analysis
networkx>=3.0
sqlparse>=0.4.0

# dsctl CLI (DolphinScheduler CLI - 本地修改版本适配 3.2.0)
# 部署时需要将 dolphinscheduler-cli 复制到 third_party 目录
# pip install -e third_party/dsctl
httpx>=0.27,<1
typer>=0.12,<1