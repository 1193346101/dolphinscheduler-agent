workflow:
  name: test_cli_create_workflow
  project: ad_monitor
  description: Test workflow created by CLI for 3.2.0
  timeout: 0
  execution_type: PARALLEL
tasks:
- name: shell_test_task
  type: SHELL
  description: 'A simple shell task'
  task_params:
    rawScript: echo "Hello from CLI 3.2.0 test"
    localParams: []
    resourceList: []
  worker_group: all_worker
  priority: MEDIUM
  retry:
    times: 0
    interval: 1
  timeout: 0
  delay: 0